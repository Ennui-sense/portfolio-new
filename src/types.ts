export type SocialsType = {
  id: number;
  href: string;
  label: string;
}

export type StackType = {
  id: number;
  label: string;
}

export type CaseType = {
  id: number;
  mp4VideoSrc: string;
  webmVideoSrc: string;
	href: string;
	label: string;
	description: string;
	posterSrc: string;
}