export type SocialsType = {
  id: number;
  href: string;
  label: string;
}

export type StackType = {
  id: number;
  label: string;
}

export type CaseType = {
  id: number;
  videoSrc: string;
	href: string;
	label: string;
	descriprion: string;
}